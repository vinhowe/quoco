#!/usr/bin/env python

import perora.plan
import sys

def run_plan():
    args = sys.argv[1:]
    if len(args) < 1:
        print("Too few arguments")
        return
    perora.plan.whats_the_plan(" ".join(args))

if __name__ == "__main__":
    run_plan()
